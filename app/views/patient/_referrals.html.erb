   		<!--   NOTES -->
        <% if referrals.count > 0 %>
            <div class-='no-page-break-inside'>
                <legend>Referrals</legend>
                <table class='table'>
       
                <% referrals.each do |letter| %>
                    <tr>
                        <td><%= link_to fa_icon_tag("eye"),patient_referral_path(@id,letter['ID'].to_i), :target => ‘_new’  %></td>
                	<td> <%= letter["LETTERDATE"] %>  </td>
                    <td><%= letter["ADDRESSEENAME"] %>  <%=  %></td>


                    </tr>


                <% end %>

            </table>

            

             
            </div>
    	<% end %>
    	<!--   END NOTES -->