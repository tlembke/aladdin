	       		           <!--   Appointments  -->
						<legend><i class="fa fa-calendar"></i>  Registers </legend>

           				Orion  

         				<%= check_box_tag 'register', @patient.id, @patient.register(1),
      data: {
        remote: true,
        url: url_for(action: :register, id: @patient.id,register:1),
        method: "POST"
      } %>

