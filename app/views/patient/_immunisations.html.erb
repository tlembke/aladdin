             <div  class= "no-page-break-inside">
                     <legend class="panel-title"><i class="fa fa-medkit"></i>  Immunsiations</legend>
       
            <div class="panel-body">
             <table class='table table-striped table-bordered table-condensed'>
                         <tr><td width=50%>Tetanus status</td><td><%= "Given on " + @patient.tetanus.strftime("%d/%m/%Y") if @patient.tetanus %><%= "No record" unless @patient.tetanus %>
                         </td><td>
                <% if @patient.tetanus_msg %>
                         <a class="btn btn-warning"> Boostrix Due </a>
                <% else %>
                        <a class="btn btn-success"> Up to Date </a>

                <% end %>

            </td></tr>
            
            </table>
            </div>
            </div> <!-- end no page break -->