							<!--   Med Changes -->
				        <% if meds.count or @changes.count> 0 %>
				        <hr>

				        <legend><i class="fa fa-exchange"></i>  Medication Changes</legend>

				        <ul>
				        <% meds.each do |task| %>
				        	<li><%= task %></li>
				        <% end %>
				         <% @changes.each do |change| %>

				        	<li><%= change['Change'] %> - <%= change['DrugName'] %> <%= change['Dose'] %> <%= change['Reason'] %></li>
				        <% end %>
				        </ul>
				    	<% end %>
	
	Please let us know if there are discrepancies between this list and that currently being dispensed<p>
				    	<!--   End Med Changes -->

